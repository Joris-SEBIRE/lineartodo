"""LinearTodo — ce qu'il te reste à faire sur Linear, dans la barre des menus macOS."""

VERSION = "1.0.0"

# Couleur d'identité de l'app, en bleu : elle ne dit rien de l'état, elle dit *quelle* app
# parle. Deux icônes voisines dans la barre, même mise en page et même vocabulaire dans les
# menus : sans cette couleur, on ouvre le mauvais menu une fois sur deux. C'est le nom d'un
# sélecteur de `NSColor`, pour que l'app suive les teintes système en clair comme en sombre.
IDENTITY_TINT = "systemBlueColor"
